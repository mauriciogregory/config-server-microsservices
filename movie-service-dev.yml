server:
  port: 8090
spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/checkpoint01
    username: root
    password: 
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create
  sql:
    init:
      mode: always
